{
  "name": "rewatchr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "deploy": "yarn build && firebase deploy --only hosting,database",
    "make-icons": "node scripts/make-icons.mjs",
    "fetch-bug-reports": "node scripts/fetch-bug-reports.mjs",
    "resolve-bug-report": "node scripts/resolve-bug-report.mjs"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "jsdom": "^24.0.0",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.5",
    "vitest": "^1.6.0"
  }
}
